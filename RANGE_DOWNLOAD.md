# Android TV 下载加速

设计参考 [Bilibili-thread-ripper](https://github.com/MrTangLuyao/Bilibili-thread-ripper/tree/23e15d5150dc6d6ae20d5361a0767b6a4e4dd284)，
仅接管点播媒体字节传输，不替换 ExoPlayer/IjkPlayer 的解码、音视频同步和播放控制。

## 设置

设置页新增「下载加速」，设置在下次进入播放器时读取。

| 选项 | 默认值 | 行为 |
| --- | --- | --- |
| 多线程下载加速 | 开 | 关闭即回到原下载链路 |
| 自动并发调度 | 自动 | 结合有效吞吐、播放码率和缓冲；低缓冲时保持恢复能力 |
| 并发连接上限 | 8 | 2/4/6/8，音视频合计，开启补救时包含一个补救槽位；不是必须占满的线程数 |
| CDN 节点模式 | 自动（全节点测速） | 自动扩展大陆及海外节点；也可指定大陆或海外节点池 |
| 慢请求补救 | 开 | 备用节点接力当前缺失的尾部，完成后取消重复请求 |
| 下载缓存内存上限 | 4 MiB | 可选 2/4/8 MiB，不含播放器缓冲、解码帧、HTTP/TLS 缓冲 |
| 下载调试日志 | 关 | `RangeAccel` 标签，不记录完整签名 URL |

大陆模式会补充华为、阿里、08c 等内置节点，海外模式包含 cosov、aliov 和香港节点。
不是仅对接口返回的地址改排序；`cosov` 不再被误判为大陆节点。
仅对可信 Bilibili 域名下 `/upgcxcode/` 的 `.m4s` 地址生成 HTTPS 候选，保留原路径及查询签名，
去掉非标准端口；不改写 HLS、直播、接口地址或任意第三方 URL。仅使用固定白名单目标域名。
所选区域节点失败/退避后仍可使用接口原地址；关闭加速后完全回到原始下载链路。
节点池参考项目的 MIT 声明随 APK 放在 `assets/THIRD_PARTY_NOTICES.md`。
浏览器全接管、网页悬浮窗、128 线程和实验性直播加速不直接移植到 TV。

为对照本次网页测试，可选择「大陆模式」「自动并发调度：手动」「并发上限：8」。
已有明确保存的设置不强制覆盖；旧版没有手动设置上限时，新默认值为 8。

## 下载路径

- 冷启动先请求 64 KiB 媒体头部，收到响应头即可 open，字节到达即可 read，无须等待整片。
  初始化请求与媒体探测分开：几 KB 的初始化/SIDX 请求只说明可用性，不记录媒体带宽。
  测速按「域名 + 媒体路径」隔离视频、音频及不同清晰度，至少 64 KiB 的媒体传输才更新带宽，
  90 秒后过期。相同文件更换查询签名可复用测速。
  首次遇到未知媒体线路时，用补救槽位与另一个域名竞速；同一域名的不同签名不充当不同节点。
  已有有效媒体测速时，即使缓冲偏低也不重复退回单路 64 KiB 门槛。
- 后续块按已测吞吐选择 128–512 KiB；只分配当前滚动窗口，不将大请求整体放入内存。
- 当前默认配置从四个视频槽位和一个普通音频槽位开始。正常请求使用优先队列，
  另留一个尾部补救槽位；所有槽位合计不超过设置上限。
- 自动模式仅统计去重后的有效字节，按约 4 秒窗口比较吞吐。缓冲低于 3 秒且下载繁忙时，
  2 秒后可提升到配置上限；只有缓冲至少 8 秒、且吞吐具有播放码率的 1.5 倍余量时才允许
  因增量不足 10% 而回退试探。空闲窗口不导致增加并发。
- 首选 CDN 根据测量结果决定，不再轮流将未知/慢节点分配给首播。
  少量位于当前读头之后的任务可探测未知备选节点，不再要求先积累 8 秒缓冲才探索。
- 约 900 ms 无进展，或当前片预计赶不上缓冲余量时，备用槽位请求尚未下载的尾部。
  低缓冲时根据首字节延迟在 350–900 ms 间提前补救；已有可读数据时不启动无必要的救援。
  两个响应只发布连续前缀，重叠字节不会重复交给解码器。
- 失败路线指数退避；失败或竞速落败的请求即使贡献过数据，也不能借另一个请求完成整块
  而清除自身失败状态。每块进入关键读取路径后的等待期限不会因不断收到少量字节而重新计时。
- 所有响应在发布字节之前核对 206、Content-Range、长度及同资源总长度。
  不支持 Range 的资源暂时停用加速。已知长度和开放式范围均可处理。
- 数据按序消费，消费完的块释放；预算不足时停止预取。当前块也无法分配时，
  回退原流式链路，不无期限等待内存。
- 重试只请求缺失字节；加速失败从已经交付给播放器的位置恢复原链路。
  超时可以回退，用户取消不能触发重新下载。
- ExoPlayer 保留 Media3 默认 LoadControl 参数。下载反馈通过 application looper
  采样，后台下载线程不直接访问 ExoPlayer 状态。
- UI 网速统计真实网络读取字节（含少量补救重复流量）；自适应控制只统计有效字节。

IjkPlayer 复用节点池和下载核心，WEB/APP 请求配置共享连接和内存预算，并反馈缓冲与码率。
关闭总开关后使用原始 DASH URL，不再经由本地代理。
直播与播放列表不走 Range 加速。

## 构建与观察

`Android Build` 在 main/codex 分支推送时只编译 Debug APK 并上传 Actions artifact，
不更新 Release 或 R2 latest。无需本地 JDK/Android SDK。

编译通过不代表电视上的性能已验证；保留总开关便于对照同一视频、同一清晰度下的
首帧等待和缓冲情况。`RangeAccel` 的 `config v=2` 记录本轮设置，`stream` 记录资源和节点池，
`request` 记录实际请求节点、首字节时间、状态码、物理/有效字节及独立请求是否完成，
`throughput` 每约 4 秒记录有效吞吐、物理吞吐、码率需求、缓冲和内存窗口。
ExoPlayer 另记录解码器名称、真实视频掉帧和解码异常，不再需要从弹幕层 FPS 猜测解码表现。
这些日志不包含完整签名 URL；竞速取消引起的请求异常应结合 `useful` 与 `complete` 判断。
纯逻辑测试覆盖节点扩展边界、签名保留、区域分类、测速置信度和低缓冲并发策略，
本轮按要求不在本地执行测试或构建；测试代码也不等价于 CDN/电视端集成验证。
